package com.cheffoafk.voiceassistant.ui.theme

import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.ui.unit.dp

// ── Shape tokens ────────────────────────────────────────────────────────────
/** Card di sezione, bottoni frasi. */
val CardShape = RoundedCornerShape(16.dp)

/** Bottoni freccia di scansione. */
val ArrowButtonShape = RoundedCornerShape(12.dp)

/** Chip / suggerimenti pill. */
val PillShape = RoundedCornerShape(50)

/** Thumb e track della scrollbar. */
val ScrollbarShape = RoundedCornerShape(6.dp)

/** Banner di avviso scansione attiva. */
val BannerShape = RoundedCornerShape(14.dp)

// ── Spacing tokens ───────────────────────────────────────────────────────────
/** 8 dp – spaziatura compatta (es. passi onboarding). */
val SpacingSmall = 8.dp

/** 10 dp – spaziatura standard all'interno delle card. */
val SpacingDefault = 10.dp

/** 16 dp – spaziatura larga tra card / padding orizzontale. */
val SpacingLarge = 16.dp

